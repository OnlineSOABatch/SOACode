<?xml version="1.0" encoding="UTF-8"?>
<wsdl:definitions name="CalOrderTotal"
             targetNamespace="http://xmlns.oracle.com/OnlineBatch25/CalcOrderTotal/CalOrderTotal"
             xmlns:wsdl="http://schemas.xmlsoap.org/wsdl/"
             xmlns:client="http://xmlns.oracle.com/OnlineBatch25/CalcOrderTotal/CalOrderTotal"
             xmlns:ns1="http://www.example.org/mywork/OnlineBatch25/CalcOrderTotal/SOA/Schemas"
             xmlns:plnk="http://docs.oasis-open.org/wsbpel/2.0/plnktype">

	<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
	TYPE DEFINITION - List of services participating in this BPEL process
	The default output of the BPEL designer uses strings as input and 
	output to the BPEL Process. But you can define or import any XML 
	Schema type and use them as part of the message types.
	~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->   
	<wsdl:types>
		<schema xmlns="http://www.w3.org/2001/XMLSchema">
			<import namespace="http://www.example.org/mywork/OnlineBatch25/CalcOrderTotal/SOA/Schemas" schemaLocation="../Schemas/OrderTotal.xsd" />
		</schema>
	</wsdl:types>

	<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
	MESSAGE TYPE DEFINITION - Definition of the message types used as 
	part of the port type defintions
	~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->      
	<wsdl:message name="CalOrderTotalRequestMessage">
		<wsdl:part name="payload" element="ns1:OrderTotalRequest" />
	</wsdl:message>
	<wsdl:message name="CalOrderTotalResponseMessage">
		<wsdl:part name="payload" element="ns1:OrderTotalResponse" />
	</wsdl:message>

	<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
	PORT TYPE DEFINITION - A port type groups a set of operations into
	a logical service unit.
	~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

	<!-- portType implemented by the CalOrderTotal BPEL process -->
	<wsdl:portType name="CalOrderTotal">
		<wsdl:operation name="process">
			<wsdl:input  message="client:CalOrderTotalRequestMessage" />
			<wsdl:output message="client:CalOrderTotalResponseMessage"/>
		</wsdl:operation>
	</wsdl:portType>

	<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
	PARTNER LINK TYPE DEFINITION
	~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ --> 
	<plnk:partnerLinkType name="CalOrderTotal">
		<plnk:role name="CalOrderTotalProvider" portType="client:CalOrderTotal"/>
	</plnk:partnerLinkType>
</wsdl:definitions>
